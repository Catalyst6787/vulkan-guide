To compile on arch, configure cmake with `cmake .. -DSDL_PIPEWIRE=OFF`
If I want to add sound later on we will need to fix this
I manually modified simdjson fastgltf dependency to compile with gcc 14. not the prettiest but it compiles
